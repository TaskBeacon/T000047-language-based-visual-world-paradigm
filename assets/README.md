# Assets

This task does not rely on a static external image bank.

- Visual stimuli are rendered at runtime with PsychoPy rectangles and text labels.
- Sentence audio is synthesized from the config text through the task voice pipeline.
- The generated figure preview lives at `task_flow.png` in the task root.
